CONFIGURE_FILE (
		"${CMAKE_CURRENT_SOURCE_DIR}/openicc_version.h.in"
		"${CMAKE_BINARY_DIR}/src/include/openicc_version.h"
		@ONLY
	)

SET( CHEADERS_OBJECT_CONFIG_PUBLIC
	openicc_config.h
	openicc_core.h
	openicc_db.h
   )
SET( CHEADERS_OBJECT_CONFIG_PUBLIC
	${CHEADERS_OBJECT_CONFIG_PUBLIC}
        PARENT_SCOPE
   )

INSTALL( FILES
	${CHEADERS_OBJECT_CONFIG_PUBLIC}
        "${CMAKE_BINARY_DIR}/src/include/openicc_version.h"
	DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/${PROJECT_DOWN_NAME})
